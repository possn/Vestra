name: Apply Vestra v5.7 fresh capital planner

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'chatgpt/v5.7-fresh-capital-planner'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/v5.7-fresh-capital-planner
          fetch-depth: 0
      - name: Apply and validate v5.7
        shell: bash
        run: |
          set -euo pipefail
          python scripts/apply_v57_fresh_capital.py
          rm -f scripts/apply_v57_fresh_capital.py
          python -m py_compile scripts/*.py
          node --check market.js
          node --check sw.js
          git config user.name "Vestra Build"
          git config user.email "actions@github.com"
          git add -- market.js market.css README.md sw.js
          git add -u scripts/apply_v57_fresh_capital.py
          if git diff --cached --quiet; then
            echo "v5.7 already applied"
          else
            git commit -m "feat: Vestra v5.7 fresh capital planner"
            git push origin HEAD:chatgpt/v5.7-fresh-capital-planner
          fi
