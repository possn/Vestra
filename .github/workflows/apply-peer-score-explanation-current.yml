name: Apply peer score explanation current

on:
  push:
    branches: [peer-score-explanation-current-20260830]
    paths:
      - 'scripts/patch_peer_score_explanation.py'
      - '.github/workflows/apply-peer-score-explanation-current.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: peer-score-explanation-current-20260830
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply guarded patch
        run: python scripts/patch_peer_score_explanation.py
      - name: Validate focused contract
        run: |
          node --check market.js
          python -m unittest tests.test_market_score_explanation -v
      - name: Commit patch
        run: |
          git config user.name "vestra-maintenance-bot"
          git config user.email "actions@users.noreply.github.com"
          git add market.js tests/test_market_score_explanation.py
          if git diff --cached --quiet; then
            echo "No patch changes"
          else
            git commit -m "ui: explain score relative to peers"
            git push
          fi
