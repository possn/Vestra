name: Apply Vestra v5.3 multi-move plan

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'chatgpt/v5.3-multimove-rebalance-plan'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/v5.3-multimove-rebalance-plan
          fetch-depth: 0
      - name: Apply and validate v5.3
        shell: bash
        run: |
          set -euo pipefail
          python scripts/apply_v53_multimove.py
          rm -f scripts/apply_v53_multimove.py
          python -m py_compile scripts/*.py
          node --check market.js
          node --check sw.js
          git config user.name "Vestra Build"
          git config user.email "actions@github.com"
          git add -- market.js market.css README.md sw.js
          git add -u scripts/apply_v53_multimove.py
          if git diff --cached --quiet; then
            echo "v5.3 already applied"
          else
            git commit -m "feat: Vestra v5.3 multi-move rebalance plan"
            git push origin HEAD:chatgpt/v5.3-multimove-rebalance-plan
          fi
