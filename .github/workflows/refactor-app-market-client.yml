name: Refactor app market client

on:
  push:
    branches: [main]
    paths:
      - 'scripts/refactor_app_market_client.py'
      - 'app-market-client.js'
      - '.github/workflows/refactor-app-market-client.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refactor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Extract shared market client
        run: python scripts/refactor_app_market_client.py
      - name: Validate JavaScript syntax
        run: |
          node --check app-market-client.js
          node --check app.js
          node --check market.js
          node --check market-data-loader.js
          node --check portfolio-collapsibles.js
          node --check portfolio-card-classifier.js
          node --check vestra-portfolio-hierarchy.js
      - name: Run architecture invariants
        run: python -m unittest discover -v -s tests -p 'test_*.py'
      - name: Validate diff
        run: git diff --check
      - name: Commit extraction
        run: |
          if git diff --quiet; then
            echo 'No changes to commit'
            exit 0
          fi
          git config user.name 'vestra-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add app.js app-market-client.js index.html sw.js tests scripts/refactor_app_market_client.py
          git commit -m 'refactor: move quote transport out of app monolith'
          git push
